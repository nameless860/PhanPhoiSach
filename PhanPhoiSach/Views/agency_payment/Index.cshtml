@model IEnumerable<PhanPhoiSach.Models.sale_payment>

@{
    ViewBag.Title = "Thanh toán đại lý";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Thanh toán - Đại lý</h2>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.agency.agency_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sale_payment_time)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sale_payment_money_pay)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.agency.agency_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sale_payment_time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sale_payment_money_pay)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.sale_payment_id }) |
            @Html.ActionLink("Xem chi tiết", "Details", new { id=item.sale_payment_id }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.sale_payment_id })
        </td>
    </tr>
}

</table>
